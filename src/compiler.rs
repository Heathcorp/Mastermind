// compile syntax tree into low-level instructions

use crate::{builder::Instruction, parser::Clause};

// probably the meat and potatoes of this rewrite
pub fn compile(clauses: Vec<Clause>) -> Vec<Instruction> {
	vec![]
}
